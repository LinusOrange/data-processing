# 结果摘要文件说明

这个文件不再存放静态的预计算结果。

在成功运行下面任一命令后：

```bash
python analysis_pipeline.py
```

或：

```bash
python analysis_pipeline.py --config config.template.json
```

脚本会在 `outputs/analysis_results_summary.md` 中自动生成与当前数据、当前配置一致的最新结果摘要。
